query fetchDGOsByLevelPathCount(
  $startLevelPath: String!
  $huc10: String!
  $startSegmentDistance: Float!
  $count: Int!
) {
  fetchDGOsByLevelPath(
    startLevelPath: $startLevelPath
    huc10: $huc10
    startSegmentDistance: $startSegmentDistance
    count: $count
    direction: DOWNSTREAM
  ) {
    id
    levelPath
    segmentDistance
    geometry
    geometrySimplified
    metrics
  }
}
