query uploadUrls($reportId: ID!, $filePaths: [String!]!, $fileType: FileTypeEnum) {
  uploadUrls(reportId: $reportId, filePaths: $filePaths, fileType: $fileType) {
    fileType
    url
    fields
  }
}
